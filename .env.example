DATABASE_URL="file:./dev.db"
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Demo payment: CHỈ bật ở dev/staging. Production bắt buộc "false"
# (getEnv() throw nếu production + true) + cấu hình PAYMENT_WEBHOOK_SECRET.
PAYMENT_DEMO_MODE="true"
NEXT_PUBLIC_PAYMENT_DEMO_MODE="true"

# --- Phase 1 production (Supabase/Neon Postgres + services) ---
# DATABASE_URL="postgresql://user:password@host:5432/lumina?sslmode=require"
# ADMIN_PASSWORD="doi-mat-khau-manh-12-ky-tu"
# PAYMENT_WEBHOOK_SECRET="toi-thieu-16-ky-tu-ngau-nhien"
# RESEND_API_KEY="re_xxx"
# EMAIL_FROM="Lumina Optics <orders@lumina.vn>"
# BACKUP_ENCRYPTION_KEY="32-ky-tu-ngau-nhien-cho-backup-encrypt"
# SENTRY_DSN="https://xxx@oNNN.ingest.sentry.io/NNN"
# UPSTASH_REDIS_REST_URL="https://xxx.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="xxx"
# --- Phase 2.3: R2 object storage (Cloudflare dashboard → R2 → API tokens) ---
# R2_ACCOUNT_ID="xxx"
# R2_ACCESS_KEY_ID="xxx"
# R2_SECRET_ACCESS_KEY="xxx"
# R2_BUCKET="lumina-images"
# R2_PUBLIC_URL="https://images.lumina.vn" — custom domain trỏ R2 bucket (public)
# TRUST_PROXY_COUNT="1" — bật ở prod sau 1 lớp proxy/LB (chống spoof XFF cho rate-limit)

# --- Phase 2: Google OAuth (Google Cloud Console → Credentials → OAuth client) ---
# Redirect URI phải khớp: {NEXT_PUBLIC_SITE_URL}/api/auth/google/callback
# GOOGLE_CLIENT_ID="xxx.apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="xxx"
# NEXT_PUBLIC_GOOGLE_ENABLED="true"
